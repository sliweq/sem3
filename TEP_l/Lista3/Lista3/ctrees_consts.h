#pragma once
#include <iostream>

std::string error_many_arguments = "Too many arguments, some of them were not used to build the tree\n";
std::string tree_space_char = " ";

int error_pro_build = 5;

int index_inceaser = 2;